# Working notes

<!--
The unbounded companion to `state.md`. This file is NOT loaded on Orient (the Hot
headline stays small so it always fits the host's inline budget), so it has NO size
limit — write as much as you need here without trimming. The agent reads it on demand
(state.md points here, and `celeborn search` indexes it). When something here hardens
into a permanent rule, promote it to `durable/`; when it becomes history, it goes to
`journal.md`.
-->

## Extended context
<!-- The fuller "what's going on" that doesn't fit the headline — recent work, why we're
     here, the shape of the current problem. Rewrite in place; don't let it become a log. -->
- <detail>

## Active constraints
<!-- Hard rules that must hold for this project. Permanent ones belong in durable/. -->
- <e.g. never commit without asking; tests must pass before moving on>

## Open threads
<!-- In-flight items, one line each. Close or promote them. -->
- <thread one>
