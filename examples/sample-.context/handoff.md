# Handoff

<!-- Regenerated by `celeborn handoff` from state.md + session.json. -->

**Branch:** feat/csv-export · **Status:** in-progress
**Focus:** CSV export endpoint for the reports page
**Next required action:** stream rows instead of buffering — current impl OOMs on >50k rows

**Open risks / threads:**
- decide CSV vs XLSX default for the "Download" button

---

### Resume prompt (paste into a fresh thread)

> Read `.context/state.md`, `.context/session.json`, and `.context/durable/manifest.md`, then
> continue from the Next required action above. Run `celeborn search "<topic>"` for anything
> older. Do not re-do completed work (see `journal.md`).
